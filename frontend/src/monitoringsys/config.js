// Dashboard Configuration
// Update these values to point to your server location

const config = {
  // Server URL - where the discovery server and API are running
  SERVER_URL: import.meta.env.VITE_SERVER_URL || 'http://localhost:5000',
};

export default config;
